
---

-   defaults:
        link:
            relink: true

-   clean:
        -   '~'

-   link:
        ~/.gitconfig: gitconfig
        ~/.gitconfig.author: gitconfig.author
        ~/.zshrc: zshrc
        ~/.bash_profile: bash_profile
        ~/.bashrc: bashrc
